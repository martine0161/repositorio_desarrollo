{
  "resource": {
    "null_resource": {
      "local_server": {
        "triggers": {
          "name": "${var.app_name}",
          "network": "${var.network}",
          "port": "${var.port}"
        },
        "provisioner": [
          {
            "local-exec": {
              "command": "echo 'Desplegando staging1 en net1:3000'"
            }
          }
        ]
      }
    }
  }
}