{
  "resource": {
    "null_resource": {
      "local_server": {
        "triggers": {
          "name": "${var.app_name}",
          "network": "${var.network}",
          "port": "${var.port}"
        },
        "provisioner": [
          {
            "local-exec": {
              "command": "echo 'Desplegando staging3 en net3:3002'"
            }
          }
        ]
      }
    }
  }
}